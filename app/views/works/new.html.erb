<% if current_user.typeuser.to_i == 3 || current_user.typeuser.to_i == 2  %>
<div class="jumbotron">
<h2>Nueva Tarea para <%= params[:name]%></h2>
<%= render 'form' %>

<a href="javascript:history.back()" class="btn btn-primary btn-lg" style="top:-45px;position:relative;">Atrás</a>
<%else%>
<h1>Acceso denegado :)</h1>
<%= link_to 'Atras', root_path, class: 'btn btn-default' %>
<% end %>