

<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre ,:style=>"width:11%;float:left;margin:0.3em 0;"%>
    <%= f.text_field :name, :class => "form-control ",:style=>"float:left;width:20%" %>
  </div>
  <div class="field">
    <%= f.label :Fecha_Reporte ,:style=>"width:11%;float:left;margin:0.3em 0.5em; " %>
    <%= f.date_select :date, {}, {:class=>'form-control date-control', :style=>'float:left;width: 10%;'}%> 
  </div><br><br><br>
   <div class="field">
    <%= f.label :Descripción ,:style=>"width:11%;float:left;margin:0.2em 0;"%>
    <%= f.text_area :desc, :class => "form-control" ,:style=>"float:left;width:62%" %>
  </div><br><br><br><br>
  <hr>
  <div class="field" style="display:none">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id, :value => params[:id_project] %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar",class: "btn btn-primary btn-lg",style: "margin:0 5em;" %>
  </div>
<% end %>
