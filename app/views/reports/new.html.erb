
<% if current_user.typeuser.to_i == 1 || current_user.typeuser.to_i == 2 %>
<h1>Nuevo Reporte para <% params[:name]%></h1>

<%= render 'form' %>

<%= link_to 'Back', reports_path, :class=> "btn btn-primary btn-lg",:style=> "top:-45px;position:relative;" %>

<%else%>
<h1>Acceso denegado :)</h1>
<%= link_to 'Back', root_path, class: 'btn btn-primary btn-lg', :style => "float:left" %>
<% end %>