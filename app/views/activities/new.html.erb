<% if current_user.typeuser.to_i == 3 || current_user.typeuser.to_i == 2 || current_user.typeuser.to_i == 1 %>
<div class="jumbotron">
<h2>Nueva actividad para <%=params[:name] %></h2>

<%= render 'form' %>

<a href="javascript:history.back()" class="btn btn-primary btn-lg" style="top:-65px;position:relative;">Atrás</a>
<%else%>
<h1>Acceso denegado :)</h1>
<%= link_to 'atras', root_path, class: 'btn btn-primary btn-sm', :style => "float:left;" %>
<% end %>